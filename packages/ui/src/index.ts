// @repo/ui — component library (empty scaffold).
//
// The design system is currently token-only — see @repo/tokens. Components
// will land here when the scope is formally expanded beyond the Figma
// "Color Palette & Type" frame (40009046:20491).
export {}
